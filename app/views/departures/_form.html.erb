<%= form_for(@departure) do |f| %>
  <% if @departure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@departure.errors.count, "error") %> prohibited this departure from being saved:</h2>

      <ul>
      <% @departure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trip_id %><br />
    <%= f.text_field :trip_id %>
  </div>
  <div class="field">
    <%= f.label :track %><br />
    <%= f.number_field :track %>
  </div>
  <div class="field">
    <%= f.label :day %><br />
    <%= f.date_select :day %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
